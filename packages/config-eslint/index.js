// Phase 0 Slice 03 — placeholder.
// Slice 04에서 @eslint/js recommended + typescript-eslint strict + import + unused-imports 주입 예정.
// 5.11: flat config 배열(`export default [...]`) 형태 유지.
export default [{}];
